// g - global (encontra todas as ocorrências)
// i - isensitive
const {texto} = require('./regex');

const expReg = /João e Maria começa em/i;

console.log(expReg.test(texto));
console.log(expReg.exec(texto));
